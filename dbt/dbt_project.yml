name: log_analytics_engine
version: "1.0.0"
config-version: 2

profile: log_analytics_engine

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages", "logs"]

models:
  log_analytics_engine:
    staging:
      +schema: analytics
      +materialized: view
    gold:
      +schema: gold
      +materialized: table
